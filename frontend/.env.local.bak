# Desenvolvimento: use a URL do backend local
# Produção (Vercel): deixe vazio para usar o proxy interno
NEXT_PUBLIC_API_URL=http://localhost:8000

# URL do backend (usado pelo proxy server-side)
# Em produção no Vercel, configure esta variável com a URL do seu backend
BACKEND_URL=http://localhost:8000
